# Turing Arena 与插件：核心需求拆解 (Platform + Plugin)

为了支撑 OKR 中“数字分身”、“Skills 产线赋能”和“插件数据回流”三大目标，我们需要将业务目标映射到**平台端（Web）**和**插件端（Client端/浏览器端）**的具体功能模块上。

以下是结合“平台功能 + 插件功能”的详细需求拆解：

---

## 模块一：平台端（Turing Arena Web）核心需求
平台端是“大脑”和“中台”，主要负责能力的**拼装、编排、API化以及数据接收与清洗**。

### 1. 数字分身构建与管理平台 (支撑 KR1)
**目标：** 将散落的能力组装成一个可以独立执行任务的“专家级 AI Agent”。
*   **知识库管理 (RAG 增强)：** 
    *   支持文档（PDF/Word/Markdown）、代码库的上传与向量化解析。
    *   支持知识库的动态更新与召回策略配置。
*   **专家 SP (System Prompt) 工作台：** 
    *   支持版本化管理，提供 Prompt 调试与对比（A/B Test）沙箱。
*   **能力编排与融合引擎：** 
    *   **核心链路：** 提供可视化或低代码界面，允许用户将“专家领域 Skills”、“平台数据 Skills”、“知识库”和“SP”串联组合，打包生成一个“数字分身”。
*   **质检工作流接入：**
    *   提供批量任务下发界面，展示分身的质检结果（如：Coding 错误高亮、修改建议）。

### 2. Skills 开放平台与 API 网关 (支撑 KR2)
**目标：** 让平台上的 Skills（如法律、Code、游戏等）能够作为独立的微服务，被外部产线调用。
*   **Skills 注册与调试中心：** 
    *   支持开发者自定义 Skill（入参/出参定义），提供在线 Mock 和联调环境。
*   **标准化 API 网关：** 
    *   为每一个审核通过的 Skill 生成标准化的 RESTful API。
    *   包含 API 鉴权（Token/AK/SK）、调用频控（Rate Limiting）、并发管理。
*   **调用大盘与监控报表：** 
    *   监控各个产线（标注、合成）对 Skills API 的调用量、成功率、延迟（Latency）及 Token 消耗。

---

## 模块二：插件端（Plugin）核心需求
插件端是“触角”，主要负责**数据采集、端侧交互以及与基模层的无缝衔接**。

### 1. 数据轨迹采集引擎 (支撑 KR3)
**目标：** 隐式地记录用户/专家在解决复杂问题时的真实思维路径。
*   **DOM/UI 事件追踪：** 
    *   无埋点记录用户的点击、滚动、文本框输入、页面跳转等核心交互。
*   **代码/文本编辑轨迹（针对 Coding 等场景）：**
    *   记录代码的复制、粘贴、删除、重构等过程（不仅是最终结果，而是中间状态）。
*   **隐私与安全脱敏：** 
    *   支持配置黑名单，自动在端侧屏蔽密码框或敏感信息的采集。

### 2. 多模态录屏模块 (支撑 KR3)
**目标：** 解决单纯文本轨迹无法还原复杂交互的问题，提供多模态数据。
*   **后台静默/半静默录屏：** 
    *   基于任务触发录屏（例如：专家接单开始质检时自动开启，提交质检报告时结束）。
*   **视频切片与分片上传：** 
    *   为了防止大文件传输失败，插件需将录屏流进行实时切片（如每 10MB/分片），边录边传至平台后端。
*   **视频与轨迹的时间戳对齐：** 
    *   （关键难点）确保采集到的“DOM 点击事件”能和“视频帧”在时间轴上精确对齐，方便后续基模的多模态训练。

---

## 模块三：端云协同（平台 + 插件 的串联场景）
这是体现闭环价值的关键，必须把平台和插件打通。

### 场景 1：专家质检的人机协同闭环 (服务于 KR1 & KR3)
*   **流程拆解：**
    1. 平台端的“数字分身”先对一批 Coding 数据进行初筛和自动质检。
    2. “人类专家”在 Web 端复核那些 AI 存疑的 Case。
    3. **插件启动：** 专家复核期间，插件全程记录专家的“修改轨迹”和“查阅文档的录屏”。
    4. **数据沉淀：** 平台回收这些数据，沉淀为 `[AI 错误判断] -> [专家正确修改轨迹]` 的高质量 RLHF（人类反馈强化学习）对齐数据。

### 场景 2：数据资产飞轮管道 (服务于基模)
*   **流程拆解：**
    1. 插件采集的原始 Json 轨迹和 MP4 视频流汇总到 Turing Arena 平台。
    2. 平台提供一个**“数据清洗工作台”**，过滤无效数据。
    3. 平台将清洗后的数据格式化为基模训练所需的格式（如 SFT 指令集、多模态图文对），推送到数据湖/模型训练平台。
